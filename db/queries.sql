-- SELECT * FROM department;

-- SELECT * FROM role;

-- SELECT employee.id,
--        employee.first_name,
--        employee.last_name,
--        employee.role_id,
--        employee.manager_id,
--        role.title AS role_title,
